---
phase: 06-policy-as-code
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/llm/test_config.py
  - tests/security/test_permission.py
autonomous: true
requirements:
  - POLC-01
  - POLC-02
  - POLC-03
  - POLC-04
  - POLC-05

must_haves:
  truths:
    - "pytest tests/llm/test_config.py -k 'rbac' -x fails with ImportError or AttributeError (symbols not yet defined)"
    - "pytest tests/llm/test_config.py -k 'secrets' -x fails (load_secrets_config not yet defined)"
    - "pytest tests/llm/test_config.py -k 'egress' -x fails (load_egress_config not yet defined)"
    - "pytest tests/llm/test_config.py -k 'profile' -x fails (load_spend_profiles not yet defined)"
    - "pytest tests/llm/test_config.py -k 'validation' -x fails (ConfigValidationError not yet defined)"
    - "pytest tests/llm/test_config.py -k 'gateway_config' -x fails (load_gateway_config not yet defined)"
    - "pytest tests/security/test_permission.py -k 'wildcard or deny' -x fails (wildcard/deny not yet in enforcer)"
  artifacts:
    - path: "tests/llm/test_config.py"
      provides: "~20 test functions covering POLC-01 through POLC-05 and integration"
      contains: "load_rbac_config, load_secrets_config, load_egress_config, load_spend_profiles, load_gateway_config, ConfigValidationError"
    - path: "tests/security/test_permission.py"
      provides: "4 new test functions for wildcard allow and deny rule enforcement"
      contains: "wildcard, deny"
  key_links:
    - from: "tests/llm/test_config.py"
      to: "argus.llm.config"
      via: "from argus.llm.config import load_rbac_config, load_secrets_config, load_egress_config, load_spend_profiles, load_gateway_config"
    - from: "tests/llm/test_config.py"
      to: "argus.security.exceptions"
      via: "from argus.security.exceptions import ConfigValidationError"
    - from: "tests/security/test_permission.py"
      to: "argus.security.permission.enforcer"
      via: "PermissionEnforcer with wildcard PolicyConfig"
---

<objective>
Write the complete RED test suite for all Phase 6 policy-as-code behaviors before any implementation exists.

Purpose: Establish the exact behavioral contracts that Plans 02 and 03 must satisfy. All tests must be importable and fail at call time (not at collection time), proving the behavior is missing.
Output: ~20 new test functions in test_config.py and 4 new functions in test_permission.py, all failing RED.
</objective>

<execution_context>
@/Users/ytandeta/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ytandeta/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-policy-as-code/06-CONTEXT.md
@.planning/phases/06-policy-as-code/06-RESEARCH.md
@.planning/phases/06-policy-as-code/06-VALIDATION.md

<interfaces>
<!-- Existing symbols tests must import. These do NOT exist yet — imports will fail RED. -->

From argus/llm/config.py (to be added in Plan 02/03):
```python
# New functions (not yet defined — tests will fail ImportError or AttributeError)
def load_rbac_config(raw: dict) -> "PolicyConfig | None": ...
def load_secrets_config(raw: dict) -> "SecretsConfig": ...
def load_egress_config(raw: dict) -> list[str]: ...
def load_spend_profiles(raw: dict, active_profile: str | None = None) -> "SpendConfig": ...
def load_gateway_config(raw: dict) -> "GatewayConfig": ...
```

From argus/security/exceptions.py (to be added in Plan 02):
```python
# New exception (not yet defined)
class ConfigValidationError(ValueError): ...
```

Existing symbols already importable:
```python
# argus/security/permission/enforcer.py
class PermissionEnforcer:
    def __init__(self, policy_config) -> None: ...
    def enforce(self, role: str, tool_name: str) -> None: ...  # raises PermissionDeniedError

# argus/security/permission/policy.py
class PolicyRule(BaseModel):
    role: str
    tool: str
    effect: Literal["allow", "deny"] = "allow"

class PolicyConfig(BaseModel):
    rules: list[PolicyRule] = []

# argus/security/exceptions.py
class PermissionDeniedError(ArgusSecurityError): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for POLC-01 and POLC-01 enforcer (test_config.py + test_permission.py)</name>
  <files>tests/llm/test_config.py, tests/security/test_permission.py</files>
  <behavior>
    test_config.py — RBAC loader tests (append after existing 5 tests):
    - test_load_rbac_config_returns_none_when_no_rbac_section: raw={} → returns None
    - test_load_rbac_config_allow_list_produces_policy_rules: rbac.roles.analyst.allow=[read_db] → PolicyConfig with PolicyRule(role="analyst", tool="read_db", effect="allow")
    - test_load_rbac_config_deny_list_produces_deny_rules: rbac.roles.analyst.deny=[delete_records] → PolicyConfig with PolicyRule(role="analyst", tool="delete_records", effect="deny")
    - test_load_rbac_config_wildcard_allow: rbac.roles.admin.allow=["*"] → PolicyConfig with PolicyRule(role="admin", tool="*", effect="allow")
    - test_load_rbac_config_multiple_roles: two roles each with allow lists → all rules present in returned PolicyConfig

    test_permission.py — wildcard and deny enforcer tests (append after existing 3 tests):
    - test_wildcard_allow_permits_any_tool: PolicyConfig with (role="admin", tool="*", effect="allow") → PermissionEnforcer.enforce("admin", "any_tool") does NOT raise
    - test_wildcard_allow_does_not_grant_other_roles: only admin has wildcard → enforce("analyst", "any_tool") raises PermissionDeniedError
    - test_deny_rule_blocks_even_with_wildcard_allow: PolicyConfig with admin allow=["*"] and deny=[delete_records] → enforce("admin", "delete_records") raises PermissionDeniedError
    - test_deny_rule_blocks_specific_tool: PolicyConfig with analyst allow=[read_db] and deny=[delete_records] → enforce("analyst", "delete_records") raises PermissionDeniedError
  </behavior>
  <action>
    Append test functions to tests/llm/test_config.py (after the existing 5 tests) and tests/security/test_permission.py (after the existing 3 tests).

    Import style for new test_config.py functions: import symbols inline within each test function body (following existing file pattern).

    For test_permission.py wildcard tests: build a PolicyConfig directly with PolicyRule objects — no YAML loading needed.

    Do NOT import load_rbac_config at module level — keep imports inside function bodies to match existing style. Tests will fail when the import raises ImportError (cannot import name 'load_rbac_config' from 'argus.llm.config').

    Run after writing: `pytest tests/llm/test_config.py -k "rbac" -x -q` — expect ImportError/AttributeError (RED confirmed). Also run: `pytest tests/security/test_permission.py -k "wildcard or deny" -x -q` — expect either AssertionError or PermissionDeniedError not raised (RED confirmed).
  </action>
  <verify>
    <automated>pytest tests/llm/test_config.py -k "rbac" -x -q 2>&amp;1 | tail -5 &amp;&amp; pytest tests/security/test_permission.py -k "wildcard or deny" -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>pytest -k "rbac" shows ImportError or AttributeError on load_rbac_config. pytest -k "wildcard or deny" shows test failures. Existing 5 test_config.py tests and 3 test_permission.py tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED tests for POLC-02, POLC-03, POLC-04, POLC-05, and integration (test_config.py)</name>
  <files>tests/llm/test_config.py</files>
  <behavior>
    POLC-02 secrets loader tests:
    - test_load_secrets_config_returns_empty_patterns_when_no_section: raw={} → SecretsConfig with patterns=[]
    - test_load_secrets_config_populates_patterns: secrets.patterns=["MY_SECRET_[A-Z]+"] → SecretsConfig.patterns contains that string

    POLC-03 egress loader tests:
    - test_load_egress_config_returns_empty_list_when_no_section: raw={} → []
    - test_load_egress_config_populates_allowlist: egress.allowlist=["api.example.com"] → ["api.example.com"]

    POLC-04 spend profiles tests:
    - test_load_spend_profiles_flat_keys_when_no_profile: spend.per_task_usd=0.10, no profiles, no env var → SpendConfig(per_task_usd=0.10)
    - test_load_spend_profiles_selects_named_profile: spend.profiles.dev.per_task_usd=0.10 with active_profile="dev" → SpendConfig(per_task_usd=0.10)
    - test_load_spend_profiles_env_var_selection: monkeypatch ARGUS_SPEND_PROFILE=staging, profiles.staging.per_task_usd=0.50 → SpendConfig(per_task_usd=0.50)
    - test_load_spend_profiles_unknown_profile_raises: ARGUS_SPEND_PROFILE=nonexistent, no such profile → raises ConfigValidationError with "nonexistent" in message

    POLC-05 validation tests:
    - test_config_validation_error_on_invalid_regex: secrets.patterns=["[invalid"] → raises ConfigValidationError with the bad pattern in message
    - test_config_validation_error_is_value_error: ConfigValidationError is subclass of ValueError

    Integration (load_gateway_config) tests:
    - test_load_gateway_config_returns_gateway_config: raw with rbac+secrets+egress+hitl sections → GatewayConfig with all fields populated
    - test_load_gateway_config_permissive_without_rbac: raw without rbac → GatewayConfig.permissions is None
    - test_load_gateway_config_empty_raw: raw={} → GatewayConfig with all defaults (no exception)
  </behavior>
  <action>
    Append all test functions to tests/llm/test_config.py (continuing after Task 1's additions).

    For monkeypatch tests: use pytest's `monkeypatch` fixture parameter directly.

    For the ConfigValidationError subclass test: import ConfigValidationError and check `issubclass(ConfigValidationError, ValueError)`.

    For the integration tests: import GatewayConfig from argus.security.gateway; import load_gateway_config from argus.llm.config.

    Do NOT import at module level — keep all imports inside test function bodies (matching existing file style).

    Run after writing: `pytest tests/llm/test_config.py -k "secrets or egress or profile or validation or gateway_config" -x -q` — expect ImportError or AttributeError (RED confirmed).
  </action>
  <verify>
    <automated>pytest tests/llm/test_config.py -k "secrets or egress or profile or validation or gateway_config" -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>All new tests fail RED (ImportError/AttributeError on missing symbols). The 5 original test_config.py tests still pass: `pytest tests/llm/test_config.py -k "not (rbac or secrets or egress or profile or validation or gateway_config)" -q` shows 5 passed.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `pytest tests/llm/test_config.py -q 2>&1 | tail -3` — original 5 pass, ~18 new tests fail
- `pytest tests/security/test_permission.py -q 2>&1 | tail -3` — original 3 pass, 4 new tests fail
- Full suite still at 244 passing (no regressions from test additions): `pytest tests/ -q --ignore=tests/llm/test_config.py --ignore=tests/security/test_permission.py 2>&1 | tail -3`
</verification>

<success_criteria>
- tests/llm/test_config.py has ~23 test functions total (5 original + ~18 new)
- tests/security/test_permission.py has 7 test functions total (3 original + 4 new)
- All new tests fail RED (ImportError or assertion failure — NOT collection-time errors)
- All 244 pre-existing tests still pass
- Wave 0 of VALIDATION.md is satisfied — every test in the per-task verification map exists
</success_criteria>

<output>
After completion, create `.planning/phases/06-policy-as-code/06-01-SUMMARY.md`
</output>
